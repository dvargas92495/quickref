echo === git status ===============================================================
git status
echo === git push =================================================================
git add .
git commit -m 'updates'
git push
echo === git status ==============================================================
git status
